//
// Created by HB on 5/20/2015.
//

#ifndef VECTORTILEMAP_SYNCHRONIZEGUARD_H
#define VECTORTILEMAP_SYNCHRONIZEGUARD_H

//#include <mutex>

#define synchronized(NULL) \
//            for (std::unique_lock<std::mutex> _lock(lock); ; _lock.unlock())

#endif
